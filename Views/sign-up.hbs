<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inscription</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes slideIn {
      0% {
        transform: translateX(-100%);
        opacity: 0;
      }
      100% {
        transform: translateX(0);
        opacity: 1;
      }
    }
    .question {
      animation: slideIn 1s ease-out;
    }
  </style>
</head>
<body class="bg-gradient-to-r from-sky-400 to-lime-300 h-screen flex flex-col">

  <!-- Contenu principal -->
  <div class="text-center p-10 bg-white rounded-xl shadow-lg w-96 mx-auto mt-6" style="margin-top: 100px;">
    <h2 class="text-4xl font-extrabold text-gray-800 mb-4">Choisissez votre rôle</h2>
    
    <div class="question text-xl text-gray-700 mb-6 animate__animated animate__fadeIn">
      <p>Quel est votre rôle ?</p>
    </div>

    <!-- Animation des options de rôle -->
    <div class="mt-6">
      <button class="bg-indigo-600 text-white py-3 px-6 rounded-full shadow-md hover:bg-indigo-700 transform transition duration-300 ease-in-out hover:scale-105 mb-4 w-full" onclick="redirectToRolePage('patient')">
        Patient
      </button>
      <button class="bg-green-600 text-white py-3 px-6 rounded-full shadow-md hover:bg-green-700 transform transition duration-300 ease-in-out hover:scale-105 mb-4 w-full" onclick="redirectToRolePage('doctor')">
        Médecin
      </button>
      <button class="bg-yellow-600 text-white py-3 px-6 rounded-full shadow-md hover:bg-yellow-700 transform transition duration-300 ease-in-out hover:scale-105 w-full" onclick="redirectToRolePage('pharmacy')">
        Pharmacie
      </button>
    </div>
  </div>

  <!-- Redirection script -->
  <script>
    // Fonction pour rediriger en fonction du rôle
    function redirectToRolePage(role) {
      if (role === "patient") {
        window.location.href = "/sign-up/patient";
      } else if (role === "doctor") {
        window.location.href = "/sign-up/doctor";
      } else if (role === "pharmacy") {
        window.location.href = "/sign-up/pharmacy";
      } else {
        alert("Veuillez sélectionner un rôle.");
      }
    }
  </script>
</body>
</html>
